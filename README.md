# 0x04-TypeScript/Task 0 – TypeScript Basics: Student Interface

This project introduces basic TypeScript concepts, including interfaces, typing, and DOM manipulation.  
The objective is to create a simple interface, store student data, and display it in a dynamically generated table using Vanilla JavaScript.

---

## Learning Objectives
By the end of this task, you should be able to:

- Define and use basic TypeScript types
- Create and use interfaces
- Work with TypeScript and the DOM
- Configure a TypeScript project using Webpack and ts-loader
- Ensure TypeScript compiles with no errors

---

## Project Structure


## How to Run the Project

### 1. Install dependencies

### 2. Start the development server

### 3. Build the project

### 4. Run tests

---

## Script Description

- `main.ts` defines a `Student` interface
- Creates two student objects and stores them in an array
- Dynamically renders a table showing each student's firstName and location

---

## Requirements

- No TypeScript compilation errors
- All code must follow TypeScript best practices
- Use DOM API (Vanilla JS) for rendering
